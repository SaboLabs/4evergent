# @4evergent/web

**Status: Future work.**

The web dashboard (agent discovery, profile, wallet status, recent activity, permissions, transaction history, capabilities) is planned as Phase 5 of the roadmap.

The agent-core and policy packages are fully usable today from the CLI/API layer without a frontend.
